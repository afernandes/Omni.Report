// The IDesignerDataConnect contract and DTOs now live in Reporting.Designer.Blazor
// itself (ViewModels namespace) so the designer assembly doesn't have to ship the DB driver
// surface area. This package only contributes the concrete implementation — see
// DesignerDataConnect.cs.
